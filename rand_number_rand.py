# Import numpy as np
import numpy as np

# Set the seed (Use seed() to set the seed; as an argument, pass 123.)
sd = np.random.seed(123)

# Generate and print random float (Generate your first random float with rand() and print it out.)
rndm_flt = np.random.rand()
print(rndm_flt)